// describe('<Characters />', () => {
//   describe('Given a list of characters', () => {
//     test('When the request is done, then stop displaying the loding indicator', () => {})

//     test('When the request is done, then display them as a list', () => {})
//   })

//   describe('Given an empty list of characters', () => {
//     test("When request gets done, then display an info's message", () => {})

//     test('When request is ongoing, then display a loading indicator', () => {})
//   })

//   test("Given an error, when getting characters failed, then display an error's message", () => {})
// })
